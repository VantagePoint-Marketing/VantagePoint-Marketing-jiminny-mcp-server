{
  "name": "jiminny-mcp-server",
  "version": "1.0.0",
  "description": "MCP proxy exposing Jiminny call intelligence data via the Jiminny Customer API, for use with clients (e.g. GoHighLevel) that can't complete Jiminny's own OAuth MCP discovery.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  }
}
